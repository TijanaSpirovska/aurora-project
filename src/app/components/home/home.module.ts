import { NgModule } from '@angular/core';
import { AppModule } from '../../app.module';
import { HomeRoutingModule } from './home-routing.module';

@NgModule({
  declarations: [],
  imports: [AppModule, HomeRoutingModule],
})
export class CakeModule {}
